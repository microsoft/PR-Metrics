# Copyright (c) Microsoft Corporation.
# Licensed under the MIT License.

---
# Checkov configuration for Super-Linter.
#
# Skip CKV_GHA_7 (empty workflow_dispatch inputs) because the only failure
# comes from the gh-aw-generated update-ci-dependencies.lock.yml, which uses
# an internal aw_context input managed by the Agentic Workflows framework.
# No editable workflow in this repo uses workflow_dispatch inputs, so this
# is effectively an exclusion of the autogenerated file.
skip-check:
  - CKV_GHA_7
