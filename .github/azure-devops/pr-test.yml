# Copyright (c) Microsoft Corporation.
# Licensed under the MIT License.

---
trigger: none

pr:
  branches:
    include:
      - main

variables:
  - name: tags
    value: multi-phased

resources:
  repositories:
    - repository: 1ESPipelineTemplates
      type: git
      name: 1ESPipelineTemplates/1ESPipelineTemplates
      ref: refs/tags/release

extends:
  template: v1/1ES.Unofficial.PipelineTemplate.yml@1ESPipelineTemplates
  parameters:
    featureFlags:
      scanAllEslintScriptExtensions: true
    settings:
      networkIsolationPolicy: Permissive,CFSClean,CFSClean2,CFSClean3
    sdl:
      sourceAnalysisPool:
        name: Azure-Pipelines-1ESPT-ExDShared-StaticIP
        os: windows
        image: windows-latest
      credscan:
        suppressionsFile: $(Build.SourcesDirectory)\.github\azure-devops\CredScanSuppressions.json
      eslint:
        enabled: true
        useV3: true
        customconfigfile: $(Build.SourcesDirectory)/eslint.config.mjs
        npmci: true
        targetsESLint: $(Build.SourcesDirectory)/src/task/**/*.ts
        break: true
        severity: Error
        enableExclusions: true
        exclusionPatterns: |
          **/*.js
    stages:
      - template: template.yml
        parameters:
          testInstance: true
