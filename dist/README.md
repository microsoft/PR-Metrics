# dist folder

This folder contains the compiled GitHub Action code. It is autogenerated via
`npm run build:package` and should not be manually modified.

Every file in this folder apart from this README is deleted and regenerated by
each build. The `Test GitHub Action` job rebuilds the folder and fails if the
result differs from the commit, so the shipped code always corresponds to the
reviewed source.
